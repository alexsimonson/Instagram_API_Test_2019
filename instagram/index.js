/**
 * @format
 */

import { AppRegistry } from 'react-native';
import HashtagSelect from './HashtagSelect';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => HashtagSelect);
